import React, { useState } from 'react';

const Project= ({ category }) => {
    if(useState === currentCategory.name('Portfolio'))
        return (
            <section>
             <h1>Color</h1>
            </section>
        );
}


export default Project;